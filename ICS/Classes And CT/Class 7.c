#include<stdio.h>
#include<math.h>
int main()
{
    /*float v,u,a,s;
    scanf("%f %f %f",&u,&a,&s);
    v = sqrt(pow(u,2) + 2*a*s);
    printf("v = %.2f",v);
    double u,a,s,v;
    scanf("%lf %lf %lf",&u,&a,&s);
    v = sqrt(u*u + 2*a*s);
    printf("v = %.2lf",v);
    double a;
    a = pow(2,5);
    printf("%lf",a);
   long double a;
    a = pow(2,5);
    printf("%.0Lf",a);
    double a;
    a= floor(4.99);
    printf("%lf",a);
    double a;
    a= ceil(4.11);
    printf("%lf",a);
    double a;
    a= round(4.51);
    printf("%lf",a);*/
     double a;
    a= round(4.49);
    printf("%lf",a);
    getch();
    return 0;
}
